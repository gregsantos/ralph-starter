shell=bash
source-path=SCRIPTDIR

# SC2034: "X appears unused" — many vars are used across functions or in subshells
disable=SC2034
# SC2155: "Declare and assign separately" — style preference, no functional impact
disable=SC2155
# SC2086: "Double quote to prevent globbing" — flagged on integer vars (safe)
disable=SC2086
